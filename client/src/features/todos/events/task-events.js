import { storeTask } from "../api.js";

// Add addEventListener() for the buttons to add tasks with
// the same status as the column then call api to make task in DB
// if done take id and render task

function addTaskEvent() {
  const addTaskBtn = document.querySelectorAll("");
}
